{
  "name": "choo-expose",
  "description": "Expose a choo instance on the window",
  "repository": "yoshuawuyts/choo-expose",
  "version": "1.2.1",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov"
  },
  "dependencies": {
    "on-idle": "^3.1.0",
    "plucker": "0.0.0",
    "remove-array-items": "^1.0.0",
    "state-copy": "^1.0.2"
  },
  "devDependencies": {
    "dependency-check": "^2.8.0",
    "nyc": "^10.1.2",
    "standard": "^9.0.0",
    "tape": "^4.6.3"
  },
  "keywords": [
    "choo",
    "expose",
    "window"
  ],
  "license": "MIT"
}
